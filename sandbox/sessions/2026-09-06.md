# Session — 2026-09-06

**Who worked today:** Manish

---

## Part 1 — Sandbox scaffolding

### Goal
Set up the `sandbox/` folder — a proper docs / sessions / code structure plus a
`CLAUDE.md` rules file, so the implementation phase starts organized.

### What happened
- Created `sandbox/` with folders: `docs/{paper,plans,research}`, `sessions/`, `code/{environment,algorithms,training,evaluation,configs,results,notebooks}`
- Copied Ayesha's synopsis into `docs/paper/` + plain-text extract (`synopsis_text.txt`)
- Copied Ayesha's `implementation_guide.md` into `docs/plans/`
- Wrote `CLAUDE.md` — persona (**Aayat**: senior partner ~20 yrs exp, honest, Ayesha's best friend), two modes (Mode A = Ayesha → warm, explain simply, push back kindly; Mode B = Manish → direct, no persona), session-start "who is working?" check, folder rules, read-only rule (`bin/`, `000aaaaaa. after approval/` untouched), coding rules
- Wrote `sessions/template.md`; README files for `sandbox/`, `code/`, `docs/paper/`

### Design decisions
- `sessions/` sits outside `docs/` (operational memory, separate from project docs)
- Persona split: Aayat only for Ayesha; direct mode for Manish
- Code structure taken from implementation guide Section 13
- No git commit / push (Manish asked)

---

## Part 2 — Review of the implementation guide + full planning doc set

### Goal
Manish asked: read `docs/plans/implementation_guide.md`, judge what's right/wrong, and
write a proper planning documentation set so we're clean on what to do.

### What happened
- **Read** the implementation guide and the DA-MAPPO + IGAT-MARL handbooks
  (`bin/91.../03_methodology.md`, `04_results.md`; `bin/9.../03_methodology.md`, `04_results.md`)
- **Assessment of the guide:** good as a beginner onboarding doc, not an implementation
  plan. Main problems: (1) PAH training method undefined + the code is naive/wrong
  (unbatched, no input normalization, reward-hacking risk unaddressed); (2) suggests
  `marlbenchmark/on-policy` as a base — too heavy for a beginner, recommend minimal
  clean MAPPO instead; (3) GPU oversold — real speedup is vectorized envs (CPU);
  (4) reward functions never defined; (5) variable-length neighbor obs unhandled;
  (6) assignment thrashing not mentioned; (7) no multi-seed / repro infra;
  (8) PyBullet deviation not resolved with supervisor.
- **Wrote 4 plan docs** (`docs/plans/`):
  - `00_master_plan.md` — the one-line claim, 7 principles, phases P0–P6, MUST / MUST-NOT lists
  - `01_roadmap.md` — phase-by-phase tasks + done-criteria, mapped to the synopsis Gantt, standing risks
  - `02_experiment_protocol.md` — metrics, N_eval=200, ≥5 seeds, method/ablation grid, the "does PAH win?" decision rule
  - `03_engineering_standards.md` — configs, determinism, pytest suite, logging, checkpointing, Kaggle workflow
- **Wrote 5 research docs** (`docs/research/`):
  - `00_problem_formalization.md` — Dec-POMDP, obs vector (31-dim, exact fields), action `(ux,uy)`, termination, mission/safety reward terms with formulas, parameter table
  - `01_pah_design.md` — fixed PAH architecture (batched, normalized), the reward-hacking analysis, 4 candidate training formulations (A: scalar reward / B: two-head advantage / C: supervised α / D: extra actor output), recommendation (build A, switch to B if it hacks), thesis diagnostic figures
  - `02_assignment_and_conflict.md` — Hungarian cost matrix + thrashing + 4 anti-thrashing options; conflict graph with proper CPA math (`t* = −(p·v)/(v·v)`), corrects the guide's t=H-only check
  - `03_baseline_specs.md` — the 4 baselines on/off table; extracted DA-MAPPO params (MLP 3×256, LR 1e-5, clip 0.2, entropy 0.1, 3M steps, reward tiers) and IGAT params (DQN, ±15°, GAT 4 heads, DCPA/TCPA); adopt-vs-change lists; why cross-paper numbers aren't comparable
  - `04_open_questions_for_supervisor.md` — 14 numbered questions needing written sign-off (PyBullet, action space, PAH formulation, seeds, IGAT baseline interpretation, moving-target model)
- Updated `CLAUDE.md` (session-start now points at the plan docs; we're in P0),
  `sandbox/README.md`, `docs/plans/README.md`, `docs/research/README.md`
- Renamed the session file `2026-09-05.md` → `2026-09-06.md` (today is 2026-09-06)

---

## Part 3 — handbook/ (simple Hinglish explanations)

### Goal
Manish asked for a `handbook/` folder: every `docs/` and `code/` file gets a plain
Hinglish explanation so Ayesha understands everything, no jargon left unexplained.

### What happened
- Created `handbook/` mirroring `docs/` and `code/` (same paths, same filenames)
- `handbook/README.md` — naming rule + index of what's explained
- `handbook/glossary.md` — ~50 technical terms in simple Hinglish (ablation, actor,
  advantage, α, baseline, CTDE, curriculum, Dec-POMDP, GAE, Hungarian, MAPPO, MSR,
  PAH, PPO, reward hacking, seed, TTC, vectorized env, ...)
- Hinglish explainer for each of the 9 plan/research docs + the synopsis
  (`handbook/paper/synopsis.md`)
- **Added the handbook rule to `CLAUDE.md`** (Section 4 Rules): whenever a `docs/` or
  `code/` file is created/changed, its `handbook/` mirror must be created/updated —
  what it is, why we need it, main points, hard words → glossary
- Also fixed stale "`marlbenchmark/on-policy` as base" references in `CLAUDE.md` and
  `code/README.md` → minimal clean MAPPO from scratch (decision from Part 2)

### Next steps
Same as Part 2's list. From now on: any new `docs/`/`code/` file → also write its
`handbook/` entry in the same commit of work.

### Key design decisions
- **Minimal clean MAPPO from scratch**, not `marlbenchmark/on-policy`
- **Custom 2D point-mass env**, action = `(ux, uy)` velocity, obs ~31-dim with fixed
  `K_nbr=4` conflict-neighbor slots (zero-pad + mask)
- **PAH:** start with Option A (α on scalar reward, matches synopsis) + an α prior;
  instrument for reward hacking; switch to Option B (two-head advantage weighting) if
  needed. **This is the #1 thing to settle with the supervisor.**
- Stage-1 success = *qualitative* DA-MAPPO replication (mechanism helps + ablation
  collapses), not their exact 90–99% numbers (different env)
- Per-step Hungarian first (match DA-MAPPO); add switching cost only if thrashing shows

### Problems / blockers
- None blocking. Biggest open item: PAH formulation needs supervisor input before P4.
- `03_baseline_specs.md` params are from the handbooks, not yet verified page-by-page
  against the PDFs — that's a P0 task.

### Test / run results
- Nothing run — still no code.

### Next steps (P0)
- [ ] Ayesha/Manish review the 9 new docs
- [ ] Verify `03_baseline_specs.md` parameter tables against the actual PDFs, add page refs
- [ ] Send `docs/research/04_open_questions_for_supervisor.md` to Dr. Faisal Rehman
- [ ] Start P1 in parallel (doesn't depend on the PAH answer): `code/requirements.txt`,
      venv, `code/environment/multi_uav_env.py` skeleton + first unit tests
- [ ] `code/algorithms/hungarian.py` (small, parallel)

### Open questions for Ayesha / supervisor
All 14 consolidated in `docs/research/04_open_questions_for_supervisor.md`. Most urgent:
- Q1 PyBullet → custom 2D env — written OK?
- Q4/Q5 PAH: α on scalar reward or on advantage combination? Second critic head allowed?
- Q11 Is qualitative DA-MAPPO replication acceptable for Task-I?
- Q12 IGAT baseline: adapt-to-MAPPO or faithful DQN reproduction?
