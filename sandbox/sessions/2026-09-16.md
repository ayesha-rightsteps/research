# Session — 2026-09-16

**Who worked today:** Ayesha + Aayat (+ Manish ne commit push kiya — e12d5dd)

---

## 1. Session goal

Stage 2a train karna: 5 drones, moving targets, 2 obstacles (curriculum), plain MAPPO (PAH OFF).
Manish ne 2026-09-15 ke baad ek critical bug fix push kiya tha — aaj usse verify karke Kaggle pe run karna tha.

---

## 2. What happened (work summary)

### Part 1 — Manish ki fix verify ki

Commit `e12d5dd` se notebook `kaggle_stage2_plain.ipynb` mein yeh changes aaye:

**Bug:** Stage 2 notebook (jo scratch se likhi gayi thi) mein do galtiyan thi:
1. `Critic(obs_dim)` — har drone apni local obs pe alag critic value leta tha. Yeh centralized nahi tha — CTDE principle toota hua tha.
2. `rews.mean()`, `lps.mean()` — 5 drones ke rewards/log_probs ek number mein average ho jaate the. Result: har drone ko same blurred gradient milta tha chahe usne achha kiya ya bura. Credit assignment destroy ho gayi thi.

**Fix:**
- `Critic(n_drones * obs_dim)` — ab ek joint global state (5×30=150 dim) leta hai, ONE value deta hai
- `RolloutBuffer.add()` — per-drone `rews` aur `lps` arrays store hote hain, koi averaging nahi
- `compute_gae` — (T, n) shape arrays pe kaam karta hai, har drone ka apna advantage

**Verification (Manish ka local test):** log_std 30 updates tak ±0.08 ke andar raha — explode nahi hua.

### Part 2 — Stage 2a Kaggle run chali

Notebook upload kari, run shuru ki. Config:
- `n_drones=5`, `n_obstacles=2` (curriculum: Stage 2a), `target_speed=1.0`
- `total_episodes=8000`, `rollout_steps=512`, `eval_every=100`

**Run results:**

| Episode | Success | Collision | Entropy | log_std | Time |
|---------|---------|-----------|---------|---------|------|
| 2000 | 100.0% | 0.0% | 2.44 | [-0.257, -0.146] | 11.8m |
| 3000 | 100.0% | 0.0% | 2.65 | [-0.218, +0.013] | 16.4m |
| 5200 | 100.0% | 0.0% | 3.05 | [-0.063, +0.289] | 26.0m |

Entropy slowly climb karti rahi (2.44 → 3.05) lekin healthy range mein rahi. log_std clamp boundary (1.5) se bahut door. Stage 2a ne clearly converge kar liya.

### Part 3 — Modulo-skip bug pakda aur fix ki

**Bug (aaj ki ghalti):** Eval condition thi `if ep_count % CFG['eval_every'] == 0:`. Lekin episodes choti thi (hard collision termination ke saath ~20-25 steps on average), isliye ek 512-step rollout mein ~20 episodes fit hoti thi. ep_count 20-20 ke jumps mein badh raha tha aur kabhi exactly 100, 200, 300... pe nahi aata tha. Result: poore 8000 episodes mein sirf **3 eval points** (ep 2000, 3000, 5200). Thesis ke liye training curve useless thi.

Same bug save condition pe bhi thi.

**Fix (`kaggle_stage2_plain.ipynb` Cell 6):**

```python
# Purana (galat):
ep_count = 0
...
if ep_count % CFG['eval_every'] == 0:     # exact multiple chahiye — skip hota tha
if ep_count % CFG['save_every'] == 0:

# Naya (sahi):
ep_count  = 0
last_eval = 0   # ← naya
last_save = 0   # ← naya
...
if ep_count >= last_eval + CFG['eval_every']:   # koi miss nahi hoga
    ...
    last_eval = ep_count
if ep_count >= last_save + CFG['save_every']:
    last_save = ep_count
```

---

## 3. Files created / changed

| File | Change |
|------|--------|
| `sandbox/code/notebooks/v3/stage2/stage2-a/kaggle_stage2_plain.ipynb` | Modulo-skip bug fix in Cell 6 (last_eval / last_save variables) |
| `sandbox/code/notebooks/v3/stage2/stage2-a/output-with-3-data-points/` | **DISCARDED** — modulo-skip bug ki wajah se sirf 3 data points; thesis mein use nahi hoga |
| `sandbox/code/notebooks/v3/stage2/stage2-a/kaggle_stage2_plain.ipynb` | Modulo-skip bug fix in Cell 6 (last_eval / last_save variables) |

---

## 4. Design decisions

### Obstacles static hain, moving nahi — Stage 2 ke liye sahi hai
Sawaal aya ke kya obstacles move karte hain. Check kiya: `_place_obstacles()` sirf `reset()` mein call hoti hai. Step mein sirf read hoti hain. Synopsis kya kehta hai: *"Stage 2: Is a game with 5 drones that move and some obstacles"* — obstacles moving ka zikr nahi, sirf existence ka. Static obstacles Stage 2 ke liye correct hain. Moving obstacles Stage 3+ territory hain.

### Entropy trend (2.44 → 3.05) — abhi monitor karein, touch mat karein
log_std slowly upar ja raha hai lekin clamp boundary (1.5) se bahut door hai. 100% success ke saath yeh acceptable hai — policy explore karti rahi aur phir bhi succeed karti rahi. Agar Stage 2b mein yeh 4+ pe jaaye toh `ent_coef` thoda reduce karein.

---

## 5. Problems / mistakes (aaj ki ghaltiyan)

### Ghalti 1 — Modulo-skip bug (main mistake aaj ki)
Eval aur save condition dono mein `% eval_every == 0` use ki. Yeh bug tab hota hai jab ek rollout mein multiple episodes khatam hoti hain — ep_count multiples of 100 ko jump kar jaata hai. Fix ke baad ab `>= last_eval + eval_every` use hoti hai. **Stage 2a dobara run karni hogi** taaki proper training curve mile.

### Ghalti 2 (reference: 2026-09-15) — 5 debugging runs wrong direction mein
Pehle run se hi entropy explode hoti rahi. Hum nay treat kiye:
- Obs normalization (sahi tha, helpful tha)
- n_obstacles=0 (synopsis ke khilaf — Ayesha ne pakda)
- Soft collision termination (worse hua)
- log_std clamp (symptom treat kiya, cause nahi)

Real cause Manish ne dhundha: centralized critic bug + credit assignment destroy. Lesson: jab core training loop ka structure galat ho, hyperparameter tuning se kuch nahi hoga.

---

## 6. Test / run results

**Stage 2a first run (modulo-skip ke saath):**
- Episodes: ~8000 (final_model.pt saved)
- Eval points: 3 (too few)
- Performance: 100% success, 0% collision consistently — model converged
- Entropy: 2.44 → 3.05 (stable, slowly climbing)
- log_std: well within safe range throughout

**Stage 2a first run output (DISCARDED):** `sandbox/code/notebooks/v3/stage2/stage2-a/output-with-3-data-points/`
- Yeh results **use nahi ho rahe** — sirf 3 data points hain (modulo-skip bug ki wajah se)
- Directory ka naam rename kar diya gaya taake confusion na ho
- `history.json`, `training_curves.png`, checkpoints — sab invalid hain thesis ke liye
- `final_model.pt` bhi is folder mein hai — lekin re-run se fresh model aayega, yeh use nahi hoga

**Stage 2a second run (VALID — COMPLETE):** `sandbox/code/notebooks/v3/stage2/stage2-a/output/`
- **76 eval points** (ep 105 → ep 7983) — proper training curve ✓
- Average success rate: ~93-95% | Range: 75% – 100%
- Average collision rate: ~5-7% | Max spike: 25% at ep 5141 (fluke — recovered immediately)
- Entropy: 2.10 – 2.81 throughout — never exploded, peaked ~ep 3500, came back down
- log_std: [-0.42, +0.09] range — never near clamp boundary (1.5)
- 15 checkpoints saved (~every 500 ep): ep508 through ep7579, + final_model.pt
- **Verdict: Stage 2a DONE. Proceed to Stage 2b.**

---

## 7. Next steps

- [x] **Stage 2a re-run** — complete. 76 eval points, proper training curve. `output/` folder mein saved.
- [x] Stage 2a plot confirmed — entropy stable, success 90-100% range
- [ ] **Stage 2b (PRIORITY NEXT — Manish confirmation pending)** — Stage 2a results share kar diye, 4 sawal puchay hain:
  1. n_obstacles: 2→5 seedha ya 3 pehle (gradual curriculum)?
  2. Stage 2a final_model.pt se load karein ya fresh weights?
  3. total_episodes 8000 kaafi hain ya zyada chahiye?
  4. ent_coef 0.003 rakhein ya kam karein?
- [ ] Entropy Stage 2b mein 4+ jaaye toh `ent_coef` 0.003 → 0.001 karein (Manish se poochen pehle)
- [ ] Supervisor Q4 FYI email — Ayesha ka pending kaam

---

## 8. Open questions

- Entropy 3.05 tak pahunchi Stage 2a mein — Stage 2b mein kitni tak jaayegi? Kab `ent_coef` reduce karni chahiye?
- Stage 2b ke liye kitne episodes? 8000 kaafi hain ya zyada chahiye (harder environment)?
- Final training curves (thesis ke liye) Stage 2a + Stage 2b dono chahiye honge — koi specific format Manish/supervisor chahte hain?
