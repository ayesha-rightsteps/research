# Session — 2026-09-05

**Who worked today:** Manish

## 1. Session goal
Set up the `sandbox/` folder — a proper docs / sessions / code structure plus a
`CLAUDE.md` rules file, so the implementation phase starts organized.

## 2. What happened (work summary)
- Created `sandbox/` with folders: `docs/{paper,plans,research}`, `sessions/`, `code/{environment,algorithms,training,evaluation,configs,results,notebooks}`
- Copied Ayesha's synopsis into `docs/paper/` and made a plain-text extract (`synopsis_text.txt`)
- Copied Ayesha's `implementation_guide.md` into `docs/plans/`
- Wrote `CLAUDE.md` — persona (**Aayat**: senior partner ~20 yrs exp, honest, Ayesha's best friend), two modes (Mode A = Ayesha → warm best-friend tone, explain simply, push back kindly; Mode B = Manish → direct, no persona), session-start "who is working?" check, project context, folder rules, read-only reference rule, session protocol, coding rules
- Wrote `sessions/template.md` — the format to follow for each end-of-session log
- README files: `sandbox/README.md`, `code/README.md`, `docs/paper/README.md`
- **No actual code / implementation written yet** — this is scaffolding only

## 3. Files created / changed
| File | Change |
|------|--------|
| `sandbox/CLAUDE.md` | new — rules + context |
| `sandbox/README.md` | new — index |
| `sandbox/sessions/template.md` | new — session log template |
| `sandbox/sessions/2026-09-05.md` | new — this file |
| `sandbox/docs/paper/` | synopsis .docx + .txt copy, README |
| `sandbox/docs/plans/implementation_guide.md` | copy |
| `sandbox/code/README.md` | new — code structure plan |
| `sandbox/code/**` | empty folders (`.gitkeep`) |

## 4. Design decisions
- **`sessions/` sits outside `docs/`** at the top level — it is Claude's operational memory, separate from project docs.
- **Drop PyBullet** — the synopsis mentions it, but the research is 2D; a custom Gymnasium env will be used. A written justification for the thesis defense still needs to go in `docs/research/` (pending).
- Code structure taken as-is from implementation guide Section 13.
- `bin/` and `000aaaaaa. after approval/` left untouched — user explicitly said not to delete or modify them.
- No git commit / push — user asked not to.

## 5. Problems / blockers
- None. Scaffolding complete.

## 6. Test / run results
- Nothing was run — there is no code yet.

## 7. Next steps
- [ ] Write the PyBullet-vs-custom-env justification note in `docs/research/` (for the thesis)
- [ ] Re-read the method sections of DA-MAPPO (`bin/91.`) and IGAT-MARL (`bin/9.`) and write a breakdown in `docs/research/`
- [ ] Python env setup in `code/`, `requirements.txt` / venv
- [ ] `code/environment/multi_uav_env.py` — first version of the 2D Gymnasium env (Step 1 in the guide)
- [ ] `code/algorithms/hungarian.py` — scipy wrapper (small, can be done in parallel)
- [ ] `code/configs/config.yaml` — hyperparameter skeleton

## 8. Open questions for Ayesha / supervisor
- The synopsis says "PyBullet simulation" — is a custom 2D env acceptable to the supervisor? (The guide recommends it, but confirm.)
- Continuous 2D space or a grid? (The guide mentions both; DA-MAPPO uses continuous velocity commands — default: continuous.)
- How many max conflict neighbors should be fixed in the observation vector? (The guide suggests ~3.)
