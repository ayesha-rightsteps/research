# Session — 2026-09-19

**Who worked today:** Ayesha

## 1. Session goal

Manish ki 18 September ki raat ki sari changes review karna, results samajhna,
aur Stage 3 ka plan decide karna.

---

## 2. What happened (work summary)

- Manish ka latest push (commit `2e7ddd4`) review kiya — H baseline 5 seeds
  (42-46) Kaggle pe run ho gaya, 3-way comparison (B4, H, M) complete hua
- Final Stage 2 results samjhe (Section 3 mein detail)
- Stage 3 ka plan decide kiya:
  - Warm-start: Stage 2b common checkpoint (sabke liye ek hi starting point)
  - Critic initialization: "Freeze-First" approach (Section 4 mein detail)
- Ayesha ne Stage 3 ke liye yes diya

---

## 3. Files created / changed

| File | Change |
|------|--------|
| `sessions/2026-09-19.md` | Ye file — aaj ka session record |
| `docs/research/06_pah_validation_preregistration.md` | DRAFT → ADOPTED (Ayesha sign-off, 18 Sep) |

---

## 4. Stage 2 final results — 3-way comparison

Numbers directly `history.json` files se verify kiye (NaN-safe):

| Seed | B4 (fixed 0.5) | M (PAH residual) | H (formula only) |
|------|----------------|------------------|------------------|
| 42 | 89.9% / 10.1% | 90.1% / 9.9% | 90.6% / 9.4% |
| 43 | 91.2% / 8.8% | 90.9% / 9.1% | 89.7% / 10.3% |
| 44 | 90.7% / 9.3% | 90.7% / 9.3% | 90.4% / 9.6% |
| 45 | 92.0% / 8.0% | 91.9% / 8.1% | 92.0% / 8.0% |
| 46 | 91.2% / 8.8% | 90.9% / 9.1% | 91.3% / 8.7% |
| **AVG** | **91.0% / 9.0%** (sd 0.69) | **90.9% / 9.1%** (sd 0.57) | **90.8% / 9.2%** (sd 0.77) |

**Verdict:** teeno methods statistically barabar — fark ~0.2%, seed spread
0.57-0.77% ke andar. Pre-registration Section 6 ke mutabik ye honest null
result hai. Stage 2 ka yahi conclusion hai.

H ke `delta_mean_abs` sab seeds mein exactly 0.0 (correct — H mein koi
learning nahi). M ka `delta_mean_abs` 0.014-0.141 range mein (residual kuch
seekha, zero collapse nahi hua, lekin outcome mein fark nahi pada).

---

## 5. Design decisions — Stage 3 plan

### Decision 1: Stage 2b common warm-start
**Kya:** teeno methods (B4, H, M) Stage 3 ke liye Stage 2b final checkpoint
se warm-start karenge.
**Kyun:** clean, apples-to-apples comparison. Har method ka apna Stage 2 final
use karne se Stage 2 ka null result Stage 3 mein carry-forward ho sakta tha.
**Ayesha ne approve kiya:** haan.

### Decision 2: Critic Freeze-First initialization
**Problem:** Stage 3 mein 8 drones honge (Stage 2 mein 5 the). Critic ka input
size `n_drones × obs_dim` hota hai — 5→8 drones se size change hoti hai, Stage
2b critic directly load nahi hoga (architecture mismatch).

Actor aur PAH Stage 2b se seedha transfer ho sakte hain (per-drone obs_dim
same rehti hai). Critic fresh random init hoga.

**Fix — Freeze-First approach:**
```
Stage 3 start pe:
  Actor  ← Stage 2b se load
  PAH    ← Stage 2b se load (M ke liye)
  Critic ← fresh random init

  Pehle ~500 episodes:
    - Actor FROZEN (koi update nahi)
    - Critic sirf train hota hai Stage 3 rollouts pe
    → Critic Stage 3 ki value function seekh leta hai
    → Actor stable rehta hai, noise nahi deta

  Episode ~500 ke baad:
    - Actor unfreeze
    - Normal training (actor + critic dono)
```

**Kyun yeh kaam karega:** yahi root cause tha jo aaj raat two-head critic mein
fail hua — fresh critic + converged actor ne noisy GAE advantages banaye jis se
entropy crash hua. Freeze-first mein critic ko "catch up" ka waqt milta hai
actor se pehle.

**500 episodes ka freeze duration:** Manish ko seed 42 pe test karke confirm
karna hoga — sahi number experiment se decide hoga. PyTorch mein `requires_grad`
toggle karne ka kaam hai.

---

## 6. Problems / blockers

- Critic warm-start ka exact `freeze_episodes` value abhi unknown hai —
  Manish ko Stage 3 ka pehla run (seed 42) isi pe test karna hoga
- B1, B2, B3 baselines abhi bhi nahi bane (Standard MAPPO, DA-MAPPO-2D,
  IGAT-style) — Stage 3 ke baad karni hain
- Supervisor email abhi bhi pending (Q5 — residual design synopsis se alag
  hai) — Ayesha ka kaam

---

## 7. Next steps

- [ ] **Ayesha:** Dr. Faisal Rehman ko email karein — PAH residual design
      change batayein (Q5 flag, `04_open_questions_for_supervisor.md`)
- [ ] **Manish:** Freeze-First warm-start implement karo — actor/PAH Stage 2b
      se load, critic frozen-training pehle ~500 ep, phir unfreeze. Seed 42
      pe test karo pehle.
- [ ] **Manish:** Seed 42 Stage 3 test pass hone ke baad poore 5 seeds (42-46)
      chalao — B4, H, M teeno, disjoint eval seeds pehle din se
- [ ] **Manish:** Stage 3 ke baad B1/B2/B3 baselines Stage 2 pe banana

---

## 8. Open questions for Manish

> Manish, Stage 3 ke liye critic initialization ka plan yeh hai:
>
> **Problem:** Critic ka input size 5→8 drones ke saath change hoga (`5×obs_dim`
> se `8×obs_dim`). Stage 2b ka critic directly load nahi hoga.
>
> **Suggested fix — Freeze-First:**
> 1. Actor + PAH: Stage 2b se load (koi issue nahi)
> 2. Critic: fresh random init
> 3. Pehle ~500 episodes: actor freeze karo, sirf critic train karo
> 4. Episode 500+ se: actor unfreeze, normal training
>
> **Sawal:**
> - Kya yeh approach Stage 3 notebook mein implement karna feasible hai?
> - `freeze_episodes` ke liye 500 sahi lagta hai ya alag value better hogi?
> - Koi aur approach soch raha hai critic size mismatch ke liye?
>
> Ayesha Stage 3 ke liye green light de chuki hain — Stage 2b common
> warm-start confirm hai. Technical detail aap decide karo.

---

## 9. Manish ka jawab — Stage 3 M notebook (seed-42 sanity test) banaya

**Who:** Manish (Section 8 ke sawal ka jawab, isi din)

**Ayesha ka feedback jo isse pehle mila:** naye notebooks ki detail session
file mein itni likhi jaaye ki Ayesha khud bhi unhe bana/reproduce kar sake,
sirf Manish pe depend na rahe. Isliye is section mein har change **exact**
likha hai, kaha se copy kiya, kya replace kiya, taaki koi bhi (Ayesha ya
koi bhi naya) isi tarah rebuild kar sake.

### Feasibility jawab (Section 8 ke sawalon ka)

1. **Freeze-first implement karna feasible hai — haan.** PyTorch mein
   `param.requires_grad = False` set karke, optimizer ko sirf unhi params
   pe banao jo abhi train karne hain. Koi architecture change nahi chahiye.
2. **Ek zaroori addition Ayesha ke plan mein:** sirf actor nahi, **PAH
   (`Δ` residual) ko bhi freeze karna zaroori hai**, warna wahi purana bug
   dobara hoga — fresh/noisy critic ka GAE signal seedha PAH ke residual
   ko bhi corrupt kar sakta hai, bilkul waisa hi jaisa two-head critic mein
   hua tha (`sessions/2026-09-18.md` Sections 9-10). Freeze ke dauran M
   effectively H jaisa behave karta hai (Δ pinned near-zero) — critic ko
   clean signal milta hai.
3. **`freeze_episodes=500`** ek reasonable starting guess hai (actor
   drone-count-agnostic hai, isliye sirf critic ko naya value function
   seekhna hai — static policy ke against fit karna, tez converge hona
   chahiye). Blind guess na rakhein — seed-42 run ke `critic_loss` curve
   (plot mein `unfreeze` line ke around) dekh ke confirm/adjust karenge.

### Naya notebook — kaise banaya (reproducible steps)

**File:** `code/notebooks/v3/stage3/var-alpha/kaggle_stage3_pah_seed42test.ipynb`

**Base:** `code/notebooks/v3/stage2/stage2-pah-v3-pah-change/var-alpha/
kaggle_stage2_pah_allseeds.ipynb` (ADOPTED Stage 2 M notebook) — Cells 1
(imports), 2 (ConflictGraph), 3 (Environment) **byte-identical copy**, koi
change nahi (in cells mein koi `n_drones` hardcoding nahi hai, sirf
constructor default `n_drones=5` hai jo Cell 5 ka `CFG['n_drones']` hamesha
override karta hai — verify kiya `grep` se).

**Cell 0 (markdown):** naya likha — Stage 3 context, freeze-first reasoning
(kyun PAH bhi freeze), disjoint-eval reasoning, config summary.

**Cell 4 (MAPPO+PAH classes):** Stage 2 M ke Cell 4 se 2 targeted changes:
1. Header comment update (ADOPTED status, Stage 3 note)
2. **Naya method `load_actor_only(path)`** `MAPPOAgent` class mein add
   kiya (purane `load_actor_critic` ke saath, hataya nahi — reference ke
   liye rakha):
   ```python
   def load_actor_only(self, path):
       ckpt = torch.load(path, map_location=DEVICE, weights_only=True)
       self.actor.load_state_dict(ckpt['actor'])
   ```
   Sirf `actor` key load karta hai, `critic` key ko chhoo bhi nahi —
   isliye Stage2b ke 5-drone critic (150-dim input) aur Stage 3 ke 8-drone
   agent (240-dim input) ke beech shape mismatch kabhi trigger hi nahi hota.

**Cell 5 (config):** naya likha, Stage 2 M ke Cell 5 se base:
- `n_drones: 5→8`, `n_obstacles: 5→10` (starting guess, Ayesha/Manish se
  confirm nahi hua abhi, Cell 5 mein hi badalna easy hai)
- `SEEDS = [42]` (sirf sanity test, poore grid `[42,43,44,45,46]` se pehle)
- **Naya `EVAL_SEED = 999`** constant — disjoint eval ke liye (neeche dekhein)
- **Naya `CFG['freeze_episodes'] = 500`**
- Warm-start path list mein ek `../` extra (`../../../stage2-b/...` —
  notebook ab ek folder aur neeche hai: `stage3/var-alpha/` vs pehle
  `stage2/stage2-pah-v3-pah-change/var-alpha/`)

**Cell 6 (training loop):** Stage 2 M ke Cell 6 se base, 3 changes:
1. `eval_env = make_env(seed=EVAL_SEED, ...)` (pehle `seed=seed` tha —
   training seed hi eval ke liye bhi use ho raha tha, jo pre-registration
   doc Section 4 ka flagged gap tha; ab hamesha fixed `EVAL_SEED=999`,
   kabhi training seed se match nahi karega)
2. `agent.load_actor_only(_warm_path)` (pehle `load_actor_critic` tha)
3. **Naya freeze/unfreeze block**, warm-start ke turant baad:
   ```python
   for p in agent.actor.parameters(): p.requires_grad = False
   if agent.use_pah:
       for p in agent.pah.parameters(): p.requires_grad = False
   agent.opt = optim.Adam(list(agent.critic.parameters()), lr=CFG['lr'])
   ```
   Phir training loop ke andar, har rollout ke baad, ek check:
   ```python
   if not unfrozen and ep_count >= freeze_episodes:
       # requires_grad wapas True, agent.opt naye se poore params (actor+critic+pah) pe rebuild
   ```
4. `plot_results()` mein `axvline(freeze_episodes)` add kiya har panel mein —
   graph pe dikhega kaha unfreeze hua, `critic_loss` ka before/after visually
   check karna easy hoga.

**Cell 7 (summary):** comparison text Stage 3 context ke liye update kiya,
baaki `summarize()` function same (architecture-agnostic).

### Verification (Kaggle pe bhejne se pehle) — do level

**1. Syntax:** sab 8 cells `ast.parse` se clean.

**2. Real logic test (naya, risky mechanism specifically ke liye)** —
throwaway venv mein, poori training nahi, sirf ye specific mechanism:
- Dummy checkpoint banaya jisme actor (`obs_dim=30`) + ek **5-drone** critic
  ho (real Stage2b jaisa), phir `MAPPOAgent(n_drones=8, ...)` banake
  `load_actor_only()` call kiya → **crash nahi hua**, actor weights
  checkpoint se match kiye (confirm kiya `torch.equal` se)
- Freeze karke ek fake `update()` call kiya (20-step fake rollout) →
  **actor UNCHANGED, PAH UNCHANGED, critic CHANGED** — exactly jaisa hona
  chahiye
- Unfreeze karke phir ek `update()` call kiya → **actor CHANGED** — confirm
  hua ki unfreeze ke baad normal joint training resume hoti hai

Sab checks pass hue. **Poori 8000-episode training loop local pe nahi
chalai** (Manish ka pehle se established pattern — bina zaroorat local mein
lambi training nahi chalani, sirf logic-level verify).

### Agla step

Manish seed-42 pe Kaggle pe chalayega (~1h, sirf 1 seed). Result dekh ke:
- `critic_loss` curve check karna (plot mein `unfreeze` line ke around) —
  `freeze_episodes=500` sahi tha ya adjust karna hai
- Agar theek laga, `SEEDS` wapas `[42,43,44,45,46]` karke poora grid
  chalega — phir H aur B4 (fixed-alpha) ke Stage 3 notebooks bhi isi
  pattern se banenge (same freeze-first + disjoint-eval mechanism, PAH ke
  bina B4/H ke liye — H ke liye bhi PAH-jaisa freeze zaroori nahi kyunki
  usme koi learnable PAH parameter hai hi nahi, sirf actor freeze kaafi hai)

**Status:** koi git commit/push nahi kiya. `n_obstacles=10` abhi Ayesha se
confirm nahi hua — flag kiya hua hai, Cell 5 mein easy to change.
